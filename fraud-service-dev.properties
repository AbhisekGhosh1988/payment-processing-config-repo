
server.port=8082
spring.application.name=fraud-service

# Kafka Configuration
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.consumer.group-id=fraud-group
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JsonDeserializer
spring.kafka.consumer.properties.spring.json.trusted.packages=com.payment.common.dto
spring.kafka.consumer.properties.spring.json.use.type.headers=true
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer
spring.kafka.producer.properties.spring.json.add.type.headers=true

# Fraud Detection Configuration
fraud.detection.enabled=true
fraud.score.threshold.high=75
fraud.score.threshold.medium=50
fraud.score.threshold.low=25

# Fraud Detection Rules
fraud.rule.velocity.transactions.per.minute=10
fraud.rule.velocity.transactions.per.hour=100
fraud.rule.amount.threshold.suspicious=5000.00
fraud.rule.country.whitelist=US,CA,UK,DE,FR,JP,AU
fraud.rule.blacklist.check.enabled=true

# Cache Configuration
fraud.cache.enabled=true
fraud.cache.ttl.minutes=60
fraud.cache.max.size=10000

# Timeouts
fraud.analysis.timeout-ms=2000
fraud.external.api.timeout-ms=3000

# Logging
logging.level.root=INFO
logging.level.com.payment=DEBUG

# Management
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=always
management.tracing.sampling.probability=1.0
management.metrics.tags.service=${spring.application.name}

# Security
jwt.secret=${JWT_SECRET:your-super-secret-key-min-256-bits-long-change-in-production}
jwt.expiration=3600000
request.signing.secret=${REQUEST_SIGNING_SECRET:your-request-signing-secret-change-in-production}

